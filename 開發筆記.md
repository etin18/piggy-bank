# 開發筆記

這份記的是**為什麼**這樣做，以及**踩過哪些坑**。

「怎麼用」看 [README.md](README.md)，「規格是什麼」看 [需求規格.md](需求規格.md)。
那兩份寫的是結果，這份寫的是過程——很多決定是看了實際的對帳單才知道要怎麼做，
不記下來，下次遇到又要重推一遍。

---

## 一、資料長什麼樣（這決定了一切）

整個 App 的設計都是照著**實際的單據**來的。三種單據長得都不一樣：

### 台幣計價基金的申購明細

```
投資幣別  TWD      計價幣別  TWD
異動性質  申購      申購匯率  1.0000     ← 匯率是 1
投資型態  小額      單位數    45.3700
申購淨值  66.1200   申購金額  3,000.00
手續費    18.00
```

→ 表單順序照它排：**申購金額 → 淨值 → 單位數**。

### 美元計價基金的部位明細

```
投資金額  30,000    單位數    24.6060
參考淨值  53.6300   參考匯率  31.4240   ← 匯率不是 1
參考現值  41,468    累計配息  2,684
參考損益  +14,152   參考報酬% +47.17%
```

→ `24.6060 × 53.63 × 31.424 = 41,468`，跟「參考現值」對得上。
→ **參考損益是含配息的**（41,468 − 30,000 + 2,684 = 14,152），
   所以 App 的主數字也用含息報酬，才對得起來。

### 美元計價基金的贖回通知

```
贖回單位數  0.4950
贖回價格    USD 200.5800      原幣金額  USD 99.29
後收手續費  USD 0.00          信託管理費 TWD 0.00
結匯匯率    USD 31.5750       入帳淨額  TWD 3,135.00
```

→ 賣出表單照它排：**單位數 → 淨值 → 結匯匯率 → 台幣金額**。

**算法的坑**：中間不能先把原幣四捨五入。
用 `99.29 × 31.575` 會得到 3,135.08，但對帳單寫 3,135。
要 `0.495 × 200.58 × 31.575 = 3,134.99` 再取整到元（台幣入帳沒有小數）。

---

## 二、幾個關鍵決定，以及為什麼

### 為什麼基金要分「單筆」和「定期定額」，ETF 不用

**基金**在銀行是兩筆各自獨立的投資明細，各有各的平均淨值，配息也分開發。
所以拆成兩個部位算成本。

**ETF** 的定期定額和自己下單都進同一個集保帳戶，券商庫存只顯示一個總股數、
一個平均成本。所以合併計算，型態只是記錄「這筆怎麼買的」，不影響成本。

用詞也跟著平台走：ETF 是「定期定額／自己下單」，基金是「小額（定期定額）／單筆」。

### 為什麼要有「持有回合」

一開始是「總買進金額 ÷ 總買進數量」算平均成本，賣光又買回就會錯：

```
買 1,000 股 @ 40 → 全賣 @ 45 → 再買 1,000 股 @ 30
錯的算法：(40,000 + 30,000) ÷ 2,000 = 平均 35
實際上：手上這批就是 30 塊買的，第一批早就結算完了
```

改成依時間切成一段一段的「回合」，**持股歸零就結束一段**，下次買進從零重新算。
已出清的回合留在持股頁下方，各自記著那段的已實現損益與配息。

配息歸屬用**除息日**判定（那天一定還持有），不是發放日——
常常是除息後賣掉、賣掉之後錢才入帳，用發放日會歸錯段。

### 為什麼主數字是「含息報酬」而不是「未實現損益」

因為對帳單上的「參考損益」就是含息的。而且實際遇到這種狀況：

```
公共事業A股：價格跌了 4.4%，但配息領了 4,892，總計還是賺 10.2%
```

原本兩個數字並排（右上角綠色的 −$1,485、下面紅色的 +$3,407），
一張卡片一個說虧一個說賺，看的人得先想一下。
現在展開後拆成「價格漲跌 ＋ 領到的配息 ＝ 合計」，關係一目了然。

### 為什麼「2025 之前」的期初不扣帳戶餘額

期初餘額是「當下帳戶裡有多少錢」，而期初持股的錢在那之前就付掉了。
再扣一次餘額會變成負的。所以勾了期初，「帳戶實扣」欄位會自動收起來，存 0。

### 為什麼手續費要有開關

預設算進投入成本（那是實際付出去的錢），但勾掉之後「價格漲跌」
就變成純粹的價格變動。兩種都有人要看，所以做成開關，
而且基準是在 `buildPositions` 那一層切換的，
持股、報表、標的明細全部跟著同一個開關走，不會東一個含、西一個不含。

### 配息有兩個口徑

- **持股頁的「累計配息」**：只算目前持有的部位（已出清那段領到的不算）
- **報表頁的「存錢筒累計配息」**：全期間全部，含已出清的

同名不同義最容易看錯，所以名字分開。

---

## 三、踩過的坑

### Google 試算表把 `0056` 存成 `56`

代號的前導零會被當數字吃掉。影響不只顯示——**ETF 自動抓價會因此查不到**。

解法是把代號欄設成純文字格式（`setNumberFormat('@')`）。但這裡有兩層：

1. **格式要先設、再寫值**。值一旦存成數字，之後再改格式也救不回前面的 0。
2. **`appendRow` 不吃整欄格式**。整欄設成純文字之後，編輯既有列沒事，
   但新增一列還是會掉 0。改成自己算列號、先設好那一列的格式、再 `setValues`。

前端也加了一層保護：同步回來的代號如果只是本機版本少了前導零（`56` vs `0056`），
保留本機的，下次寫入會把正確的推回去。

### 開盤前抓到的不是收盤價

證交所 MIS 的即時報價，欄位取用順序是 `z`（成交）→ `y`（昨收）→ `pz`（前一筆）。

**順序不能換**：開盤前 8:30～9:00 是試搓，`pz` 會是模擬撮合價。
實測 0056 拿到 55.8，但官方收盤價是 **55.75**。

### 從面板點進去編輯，存檔變成新增一筆

`openSheet` 會先關掉目前的面板，而 `closeSheet` 順手把 `state.editing` 清成 null，
於是從「標的明細」點某筆進去編輯，存檔時找不到原本那筆，變成新增。
從記錄頁點就沒事（那時沒有面板開著）——所以很難重現。

同一個坑也讓刪除鍵按下去沒反應（刪除也是吃 `state.editing`）。

### 改版後第一次重整還是舊的

Service Worker 對程式檔採「先用快取、背景更新」，所以改版後要重整兩次才會更新。
使用者只會覺得「明明修好了怎麼還是壞的」。
改成 `.js`/`.css` 網路優先、離線才退回快取。

### 版本警告叫人做不必要的事

原本前後端版號要求完全相等，所以只改前端也會冒出「後端版本不符，請重新部署」。
這種警告出現幾次之後就沒人理了，真的該更新時反而會漏掉。

改成前端記著「後端最後一次真的需要重新部署的版本」（`REQUIRED_API_VERSION`），
只有低於它才警告。

### 新增欄位會讓舊資料錯位

在資料表中間插入欄位（例如「計價幣別」插在第 5 欄），
舊的試算表還是 8 欄，新程式照 9 欄寫入就會錯位——
表頭寫「配息頻率」、底下卻是計價幣別。

`ensureColumns` 會比對表頭，缺的欄位**插在正確位置**，既有資料跟著右移。

### 測試自己的盲點

驗證「有沒有誤接管下拉手勢」時，原本檢查 `transform` 有沒有位移。
但放手後 inline 樣式會被清掉，「拖過又還原」跟「從沒拖過」長得一模一樣，
所以測試一直是綠的。改成數 `preventDefault` 的呼叫次數才抓到問題。

### 同一種盲點：數字全對，只有亮燈是錯的

明細頁的篩選是這個 App 第一組**多選** chips。而 `bindEvents()` 裡有一個對所有
`.chips` 生效的通用處理器，行為是**單選**——先清掉同組全部的 `is-active`、
再點亮剛被點的那顆。表單裡的型態、單位、主題本來都是單選，那樣寫沒問題。

兩個處理器綁在同一個容器上，執行順序讓症狀變得很難懂：
多選那邊先跑（改 state、重繪、亮燈正確），通用的後跑再把亮燈改回單選。
於是 **state 對、篩選對、小計每一個數字都對，只有畫面上亮錯顆**——
亮著的永遠是最後點的那一顆：

```
state=["sell","dividend"]   畫面=buy:亮  sell:暗  dividend:暗
```

回報進來時是「我只按了賣出，為什麼還有顯示配息」，但其實反過來：
配息真的還勾著，只是那顆燈沒亮。找錯方向會一直去翻小計的計算，那邊根本沒問題。

修法是多選那組標 `chips--multi`，通用處理器改成 `.chips:not(.chips--multi)` 放行。

測試沒抓到的原因跟上一條同一類：**驗了算出來的數字，沒驗畫面上的狀態**，
而「數字全對」正是這個 bug 的特徵。現在 `checkChips()` 會在三個篩選狀態下
比對 `state.ledger` 跟實際亮著的 chip。

---

## 四、故意不做的事

### 基金淨值自動抓

查過了：

- **境內基金**（投信投顧公會 SITCA）可以抓，一次 334 檔、3 秒
- **境外基金**（基金資訊觀測站 fundclear）的 API 只有績效、規模、銷售統計，
  **沒有每日淨值**

你手上的富蘭克林是境外系列，只能爬 MoneyDJ、鉅亨網那類第三方網站。
那種一改版就壞，壞了還不會馬上發現——**看到的淨值是舊的，損益就默默錯了**。
這種錯比沒有功能更麻煩，所以維持手動填。

### 台銀匯率自動抓

`rate.bot.com.tw` 的 CSV 被 Cloudflare 擋掉。而且匯率跟淨值是一起抄的，
單獨自動抓一半意義不大。

### 已實現損益不受手續費開關影響

那是歷史結算，實際發生的結果就是含手續費。開關只作用在目前持有的部位。

---

## 五、分開看與明細頁（v21）

兩件事，都做完了：報表頁多一個「分開看」，把 ETF 和基金並排比；
另外開第四個分頁「明細」，用月份框期間、勾要看的種類。
設計稿連同截圖在 `shots/設計稿-分開看與明細.html`
（`shots/` 不進版控，原始檔是 `shots/mock/設計稿.html`，直接引用 `www/app.css`）。

**兩頁的分工**

- **報表頁**：年份切換留著，卡片標題右邊加 [合計]／[分開看]。分開看時配息與投入損益
  都變 ETF／基金 兩欄，每月配息圖從堆疊改雙柱。
- **明細頁**：期間用月份範圍，「看什麼」勾買進／賣出／配息，「型態」勾定期定額／單筆
  ——型態只作用在買賣上，配息一律全收（ETF 的配息本來就不分型態）。
  清單重用 `entry` 元件，點一下就能改那筆。

入金出金不進明細頁：它沒有標的、沒有型態，對比時只能靠帳戶歸邊，
跟其他三種不是同一個層級的東西，混在同一排 chip 裡語意會亂。

### 平均每月的分母維持「已過月數」，不改成「持有月數」

原本想改：ETF 2025/12 才開始，2025 年的配息 ÷12 會顯得只有基金的零頭。
但那正是要看的事實——**那一年確實只投了一點點在 ETF**。
分母是「這一年」而不是「持有了多久」，看的是現金流，不是配息能力。

所以 `monthsElapsed()` 不動，但要把分母印在畫面上（今年 ÷9、去年 ÷12），
免得兩個年份的數字被拿來直接比。

### 配息率的分母要取「該年度最後一天」的投入成本

分子是該年度的配息，分母若用「現在」的投入成本，看 2025 年就變成拿去年的配息
除今年的持股——中間賣掉什麼，那個百分比就沒意義了。

所以 `buildRounds()` 要能算到某一天為止。那是成本計算的核心，
改完務必跑 `screenshot.js`（期望值是手算的）。

### 為什麼分開看要把圖從堆疊換成雙柱

同一組數字，堆疊圖每根柱都是滿的，看起來像每個月都穩定進帳；
雙柱才看得出 ETF 是季配、有三個月根本沒有，而基金月月都有但每次少。
要比較兩邊，這個差別本身就是答案的一部分。

### 雙柱要靠左右位置分辨，不能只靠顏色

`--etf` #7fb6d9 和 `--fund` #c3a5dd 丟進色差驗證：正常視覺 ΔE 只有 9.6
（15 以下就算難分），紅綠色盲下剩 2.0，等於同一個顏色。
換成 ink 版加深也救不回來，藍紫這一組本質如此。

堆疊圖靠「上下位置固定」還撐得住，雙柱就得補第二層辨識——
固定「左 ETF、右基金」並寫進圖例。配色本身不動。

### 「投入」有兩個口徑，名字要分開

- **報表頁「投入成本」**：目前持有的部位總共花了多少（賣掉的會退出去）
- **明細頁「期間買進」**：這段期間實際從帳戶扣走多少

跟前面「配息有兩個口徑」同一個道理，同名不同義最容易看錯。

手續費開關（`state.excludeFee`）兩頁都吃：報表頁也放一顆同款 toggle，
明細頁則是含手續費取「帳戶實扣」、不含取「金額」。
切到不含時，明細頁的淨流出就不等於帳戶實際變動，那個模式下要補一行差額說明。

### 「沒記型態」不能當成「小額」

`normalizeStyle()` 把空值當小額，那是為了讓基金的部位有地方可掛——
一筆沒填型態的基金交易總得歸到某一邊去。

但明細頁不能照抄。ETF 早期的紀錄很多是空的（型態是後來才加的欄位），
把它們算進「定期定額」，數字看起來完全合理卻是錯的。
所以另外寫了 `styleOf()`：沒填就是沒填。

篩選規則也跟著調整——**兩種型態都勾＝不篩**，沒記型態的也留著；
只勾一種時它們會被排掉，這時要數出來講一句「另有 N 筆沒記型態」。
測試資料裡 00919 那兩筆買進正好沒填，剛好驗到這條。

### 明細頁的測試要先把資料清回原狀

`screenshot.js` 是一條龍跑下來的，中間那段「從明細刪除」會**真的刪掉一筆交易**
（在驗 `state.editing` 那個坑）。明細頁的測試排在它後面，
所以照原始 SEED 手算的期望值全部差那一筆——而且差的金額很合理，
乍看只會以為是自己算錯。

與其讓期望值去遷就「前面剛好刪了什麼」，不如在這段開頭重灌一次 SEED、
順便清掉 `pb.ledger`，讓這頁從固定的已知狀態開始。報表頁那段本來就是這樣做的。

---

## 六、要挑視覺設計時，做對照頁

不要只用文字或 ASCII 草圖描述版面差異——卡片感、間距、配色這些用講的很難判斷。
做法是**產生多個變體的截圖，組成一頁並排的 HTML，直接開起來看**：

```
1. 寫一支腳本，用注入 CSS 覆蓋產生變體（不要先改原始碼，挑完才正式做）
2. Playwright 逐個套用、截圖到 shots/
3. 產生一頁 HTML 把圖並排，每張配一行說明差異在哪
4. open 開起來
```

持股頁改版時就是這樣做的：先出六種收合樣式（現在的、表格行、色塊、對帳單、
極簡、報酬率優先），挑中「表格行」之後，再出三種展開方式讓它配。
兩輪就定案，比來回描述快很多。

幾個要點：

- **變體用 CSS 覆蓋**，`page.addStyleTag()` 就夠，必要時搭配 `page.evaluate()` 改 DOM
- **互動後的狀態也要截**（展開、開啟面板），不是只截第一眼
- 對照頁可**左右捲動**，圖夠大看得清楚（330px 寬左右）
- 挑完之後問「不喜歡哪裡」來收斂，比重做一輪快

腳本不用留在 repo 裡，寫在暫存目錄跑完就好；截圖和對照頁放 `shots/`（在 .gitignore 內）。

---

## 七、漏記提醒

整本簿子唯一會主動開口的地方。做它的理由是：所有數字都建立在「記錄是完整的」，
而漏記一筆**不會產生任何矛盾** —— 年度配息少一塊、平均每月跟著低、配息率也低，
畫面上卻完全看不出來。不像算錯會有兩個數字互相打架，它只會安靜地給一個很合理的錯數字。

設計定案之後，把真實試算表的資料抓下來跑了一遍（快照放 `shots/`、`www/_snapshot*`，
都不進版控）。**三個地方是那一跑才知道要改的**：

### 用除息日推算，不是發放日

真實資料裡除息到發放的落差：

| | 除息 → 發放 |
|---|---|
| ETF | 20～26 天 |
| 基金 | 5～7 天 |

而除息日本身極規律 —— 某檔季配基金連續四季都在 19 號。
用發放日推算，間隔會忽長忽短，寬限期就得開更大才不誤報，
真的漏記反而更晚才抓得到。

### 基金要照部位分開檢查

第一版把一檔的配息混在一起看間隔，於是把這個報成「同月兩筆」：

```
黃金基金 2025-12-19   單筆  24.606 單位 × 3.47053 → $2,684
黃金基金 2025-12-19   小額   1.976 單位 × 3.47053 → $216
```

那是單筆與定期定額**各配一次**，規格裡本來就寫了「配息也分開發」。
要照 `positionKey()` 同一套切法分開檢查。

### 從沒配過的要等兩個間隔

第一版用「一個間隔 ＋ 1 個月」，把一檔「季配、5 月買、4 個月沒配息」的 ETF 報了出來。
但新標的還沒輪到第一次除息是常態，而第一次配息的時間點無法從資料推算。
改成兩個間隔（季配就是滿 6 個月）才問。

**改完這三點，真實資料裡 10 檔會配息的標的跑起來零誤報。**

### 寬限期為什麼開這麼寬

間隔的一半、最少 30 天 —— 月配要整整漏掉一期才會講話。
理由就是前面「版本警告叫人做不必要的事」那條：
警告一旦常出現就會被當成背景雜訊，真的該注意時反而會漏掉。寧可晚三週講。

### 猜錯了要有出路

那一期本來就沒配（停配、或配 0）的話，記一筆 0 元配息就補起來了。
不另外做「忽略清單」那種只存在本機、換台手機就失憶的東西。

### 一個順手補掉的坑

`buildPositions()` 回傳的物件裡 `trades` 是陣列，但同名的 `dividends`
**是數字**（目前回合的配息金額）—— 展開進來的那個同名陣列被它蓋掉了。
名字看起來對稱、型別卻不同。要原始紀錄現在得用 `dividendRows`。

## 八、照片主題（霧霾與森林）

外觀多了兩個選項，底都是一張照片，元件全部做成玻璃。
它們跟淺色／深色不一樣——**那兩個是換顏色，這兩個是換底**，所以整套規則都不同。

| | 底圖 | 玻璃 | blur |
|---|---|---|---|
| **霧霾** | 霧中山村 | 水感：極淡的面板，看得到後面 | 2px |
| **森林** | 向日葵田 | 果凍：墨綠面板，靠自己撐對比 | 18px |

**兩個主題共用同一份元件樣式**，差別全部收在一組變數裡
（`--photo`、`--glass`、`--glass-filter`、`--glass-sheen`…，定義在 app.css 最上面）。
要加第三張照片，複製一組填值就好，元件那段不用動。

選 blur 要看照片：霧景是大片低對比色塊，2px 的水感玻璃壓得住；
向日葵的花瓣葉子是高頻細節，同樣 2px 會讓小字糊在花上面，所以森林才用 18px 的果凍。

### 水感不是把面板調透明

前兩版都在調面板的透明度，做出來只是「變淡」，不是玻璃。
真正撐起水感的是這四件事：

| | 做法 |
|---|---|
| 背景幾乎不模糊 | `blur(2px)`。試過 12～20px，那是毛玻璃，看不到後面就沒有「隔著水看」的感覺 |
| 邊緣一圈發光玻璃管 | 三層 `inset` 陰影疊出厚度，元件的立體感全靠它 |
| 表面凸起高光 | 由上往下的白色漸層，像水珠鼓起來 |
| 外發光 ＋ 深投影 | 元件浮在照片上，不是印在上面 |

面板底色（`--glass`）只有 `.06`，濃一點就變回毛玻璃。

### 真的折射做不到，別再試了

參考圖裡玻璃內的背景是**被折射位移**的。CSS 要做到得靠
`backdrop-filter: url(#svg濾鏡)` 搭配位移圖，實測結果：

- Chrome 吃得下這個語法，但 `feImage` 引用不到同一份文件裡的位移圖
- 套上去之後**圓角會失效**，卡片變回方的
- iOS Safari 的支援更不確定

為一個效果賭整個 App 的圓角不划算。市面上那些 liquid glass 的設計稿
其實也沒有真的折射，一樣是 `inset box-shadow` 堆出來的。

### 底是照片，文字就只能是白的

深色字壓在會變動的照片上讀不了——同一個字捲到不同位置，對比是會變的。
所以照片要壓暗（`body::after` 那層），文字轉白系。

量到的對比（從畫面取像素，不是理論值）：

| | 霧霾 | 森林 |
|---|---|---|
| 標的名稱 | 8.00（最差 6.37） | 8.35（最差 7.40） |
| 日期明細（`--muted`） | 4.74（最差 3.78） | 4.09（最差 3.51） |
| 分區標題 | 9.90（最差 9.25） | 5.98（最差 5.70） |
| 頂欄標題 | 12.54（最差 12.36） | 10.21（最差 9.91） |

日期那行兩個主題都還在及格線下。要救就把 `--muted` 往白推一階
（霧霾 `#cfdcd4` 左右），代價是主次文字的層次變平。目前選擇維持層次。

**底圖換一張，這些數字就得重量一次。**向日葵那張原本不壓暗（果凍的墨綠面板自己撐），
量出來標的名稱只有 5.0、日期 2.6；補一層很淡的壓暗（`.18/.30`，比霧霾的 `.38/.48` 輕）
就回到 6.1／3.2，而果凍的通透感還留著。

### 三個坑

**表單面板不能通透。** 它蓋在整頁內容上，2px 的玻璃會讓底下的清單直接穿上來
跟欄位疊在一起，填數字時看不清自己在填哪一格。所以 `.sheet` 底色給到 `.94`、
blur 拉重，只留邊緣的玻璃管維持語彙。**分頁列與頂欄同理**（`--glass-bar` 是 `.88`），
太透的話下面那行字會穿出來跟分頁標籤疊著。

**圓角不能跟著放大。** 一開始 `--radius` 開到 28px，持股那種一行高的列直接變成藥丸。
收到 20/14（淺色主題是 18/12），玻璃需要一點圓潤，但就這樣而已。

**果凍不能套水感的殼，會撞臉。** 兩個主題共用元件那段之後，
森林一度看起來就是暗一點的霧霾。差別不在顏色，在兩個地方：

- **面板漸層**（`--glass-face`）：水感在面板上鋪一層由上往下的白色漸層，
  表面才會鼓起來；果凍是**平的**，一整片均勻的墨綠
- **陰影層數**（`--glass-shadow`）：水感四層 inset ＋ 外發光；
  果凍只有兩層（細的上緣高光 ＋ 從下緣往上收的光），沒有內圈線也沒有發光

把這兩樣硬套成同一組，果凍就會長出水感的立體感。所以它們是整組吃變數的，
不是共用一套數值。順帶一提拿掉那層白漸層之後，森林的對比還變好了
（標的名稱 6.1 → 8.4）—— 白漸層本來就在稀釋卡片的暗度。

**白色的玻璃疊起來會越疊越亮，而且加濃只會更糟。**
霧霾原本的 `--glass` 是 `rgba(255,255,255,.06)`（很淡的白）。
單層還好，但明細頁的 `entry` 包在 `card` 裡是兩層白疊在亮霧上，
灰字掉到 **2.74**，幾乎看不見。

直覺是「把玻璃加濃一點擋住背景」，實測反而更差（2.74 → **2.14**）——
加濃白色等於把底弄得更白，淺色字當然更糊。
**要救得讓玻璃帶暗色**：改成 `rgba(16,28,22,.20)` 之後跳到 5.10，
而且 `.20` 比森林的 `.34` 淡，通透感還在。

換句話說，暗底配白字的主題，玻璃的 tint 就該是暗的；
白色 tint 只在「背景本來就暗」時才安全。

**白框會比內容還搶眼。** 水感的厚度是靠邊緣的白線和高光做出來的，
但一頁十幾張卡片各鑲一圈亮白邊，視線會先被那些框吸走，字反而變次要。
霧霾的 `--glass-line` 和整組 inset 高光後來砍掉約一半（`.50 → .28`、`.95 → .55`），
厚度感還在，讀起來安靜很多。**順帶一提對比也跟著變好**
（日期明細最差 2.78 → 3.78）—— 那些白色高光本來就在稀釋卡片的暗度。

**把按鈕收編進玻璃，會一起蓋掉「選中」的樣子。** `.btn` 和 `.yearbar__btn` 進了玻璃清單之後，
`.btn--primary`（儲存並測試）、`.yearbar__btn.is-active`（被點的那個年份）
原本的金色實心底就被蓋成跟旁邊一樣的玻璃，看起來像沒被按下。
**在玻璃底上，半透明一律會被讀成「沒選中」**——選中和主要動作都要實心金色才分得出來。

**卡片化之後內容會貼邊。** 持股頁本來是「透明列＋分隔線」，
`.hold__row` 的左右 padding 只有 **2px**——沒有框的時候內容對齊頁面邊界是對的，
一旦包進玻璃卡片，展開箭頭和金額就貼在框上了。`.hold__detail` 同樣是 2px。
兩個都要在霧霾主題下補回內距。

### 效能

`backdrop-filter` 在手機上不便宜，而這個主題連列表每一列都有。
真的滑起來會掉幀的話，**第一個該拿掉的是 `.entry` 的 `backdrop-filter`**——
blur 只有 2px，拿掉幾乎看不出差別，但少了幾十個合成層。

### 順手修掉的既有問題

`.btn--quote`（更新 ETF 現價）寫在第 1034 行，`.btn` 寫在第 1901 行，
兩者特異性相同、後者勝，所以 `.btn--quote` 的 `--accent-weak` 底色**一直是無效的**，
實際顯示的是 `.btn` 的白底。淺色主題下看起來像正常的白色按鈕所以沒人發現，
換到照片底上才露餡（變成一塊突兀的白方塊）。

### 霧霾整套配色都離開了金色

一開始兩個照片主題共用同一組琥珀金（那是淺色主題的個性），
但金色壓在灰藍的霧景上太搶，一頁裡最亮的東西全是選中狀態。後來整套換掉：

| | 原本 | 現在 | 為什麼 |
|---|---|---|---|
| 選中／主要動作 | 金 `#ffd97a` | 淺藍 `#9ac6ff` | 跟霧景同色溫 |
| 賺 | 紅 `#ff9384` | 金黃 `#f0c864` | 見下 |
| 賠 | 綠 `#79e0aa` | 天藍 `#60c0e3` | 見下 |
| 摘要卡 | 金色漸層 | 草綠漸層 | 記錄頁唯一有顏色的卡片，不再用金色搶 |
| 漏記提醒 | 黃 `#ffd464` | 琥珀橘 `#f2a65a` | 讓位：跟新的賺錢金黃只差 6.9 會撞 |

**漲跌不用台股的紅漲綠跌，是有數據的**：

| | 正常 | 紅色盲 | 綠色盲 |
|---|---|---|---|
| 紅 × 綠（其他主題） | 82.2 | 19.9 | 17.0 |
| 金黃 × 天藍（霧霾） | 83.7 | **76.7** | **89.5** |

黃配藍是色盲最安全的一組，紅配綠正好是最危險的。
代價是**同一個 App 裡「賺」的顏色會因主題而異** —— 淺色／深色／森林仍是紅漲綠跌。

### 還沒解的：霧霾有三個藍

換完之後藍色用在三個地方，兩兩量下來有兩組偏近：

| | ΔE |
|---|---|
| 選中淺藍 × ETF 天藍 | 14.8 ⚠️ |
| ETF 天藍 × 賠錢天藍 | 13.7 ⚠️ |
| 選中淺藍 × 賠錢天藍 | 19.7 ✅ |

夾在中間的是 **ETF 的藍**。位置上還分得開（ETF 是名字前的小圓點和圖表柱，
賠錢色是右邊的金額，選中色是實心按鈕），但在「分開看」那頁，
ETF 欄的底色和欄裡的賠錢金額會藍上加藍。

要解的話就是 ETF 讓位換成紫（`#c3a5dd` 跟三個藍都差 28 以上），
但那就回到第五節說的藍紫問題 —— 紫配綠在色盲下反而不如現在。先留著。

### 摘要卡的白字對比偏低

霧霾 3.07（最差 2.75）、森林 3.78（最差 3.71），兩個都在及格線下。
原因是摘要卡有自己的彩色底（草綠／金色），白字壓在中亮度的彩色上本來就吃虧。
要救就是把底色加深，或把字從白換成深色 —— 但那會讓這張卡失去「亮起來」的效果。
目前選擇維持視覺，**這是整個主題唯一沒達標的地方**。

### 兩個照片主題的 ETF／基金 都換掉了紫色

第五節記過藍紫那組在色盲下的問題（綠色盲 ΔE 只有 1.2，等於同一個顏色），
所以雙柱圖得靠「左 ETF、右基金」硬撐。兩個照片主題都改掉之後好很多：

| | 正常 | 紅色盲 | 綠色盲 |
|---|---|---|---|
| 藍 × 紫（淺色／深色留著） | 28.0 | 9.0 | **1.2** |
| 藍 × 綠（霧霾） | 62.8 | 60.2 | **59.4** |
| 黃 × 綠（森林） | 63.8 | 41.2 | **55.1** |

原本擔心「黃配綠」是色盲最容易垮的組合，但那說的是**紅配綠**；
黃和綠的明度差夠大，色盲下靠明暗就分得開。

選色時有兩個顏色要閃開，都驗過距離：

- 基金的綠不能像 `--loss` 的綠（那是**賠錢**的顏色）。ink 版取偏黃綠的，
  霧霾 `#b0dd9c` 差 18.7、森林 `#9ad98a` 差 16.7，都不會讀成賠錢
- ETF 的顏色不能像 `--accent` 的金（那是**選中／主要動作**）。
  森林的黃 `#ffd84d` 跟 `#f5cd6a` 差 17.2；霧霾是藍，本來就不衝突

左右位置和圖例照樣留著，顏色只是第二層辨識。

### 底圖

`www/bg-haze.webp`（800×1199、47KB）與 `www/bg-forest.jpg`（800×1200、90KB），
兩張都有進 Service Worker 的 SHELL——少了它主題會變成純色塊，玻璃沒有東西可以透。

換圖要注意兩件事：

**照片的明暗決定白字讀不讀得到。** 換一張亮的就得同時加重 `body::after` 的壓暗，
不然整頁的字會一起變糊。換完跑一次對比量測再決定壓多少，用眼睛看不準。

**細節多寡比明暗更關鍵。** 向日葵那版的花瓣和葉子是高頻細節，
透過 2px 的玻璃還是一片花，小字壓在上面就糊了；
霧景是大片的低對比色塊，同樣的透明度反而清楚。選圖優先找霧、遠景、淺景深這類。

---

## 九、測試

三支，都要先開著 `python3 scripts/serve.py`：

| 腳本 | 顧什麼 |
|---|---|
| `screenshot.js` | 灌一份手算過的假資料走完主要流程，比對平均成本、帳戶餘額、配息統計等數字，順便截圖 |
| `test-edge.js` | 故意灌不乾淨的資料：超賣、缺成本、孤兒交易、極端數字、全新使用者 |
| `test-sheet-drag.js` | 下拉關閉手勢，用 CDP 送真觸控事件 |

另外 `test-quotes.js` 不需要 server，直接連證交所驗證報價抓取。

**改了計算邏輯就跑 `screenshot.js`**，它的期望值是手算出來的，
註解裡有算式，數字對不上會直接指出哪一項。
